---
phase: 04-output-and-display
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - screener/display.py
  - tests/test_display.py
autonomous: true
requirements: [OUTP-01, OUTP-02]

must_haves:
  truths:
    - "Screening results display as a formatted rich table with 10 columns: Symbol, Price, AvgVol, MktCap, D/E, Margin, Growth, RSI, Score, Sector"
    - "Table rows are numbered and sorted by score descending"
    - "Scores are color-coded green/yellow/red based on thirds of the actual score distribution"
    - "Numbers are compactly formatted: market cap as $2.1B, volume as 3.2M, price as $24.50, percentages with 1 decimal"
    - "A stage summary panel shows counts at each stage: Universe, After bars, Stage 1, Stage 2, Scored"
    - "A per-filter breakdown table shows a waterfall of each filter's removal count and remaining count"
    - "Zero passing stocks shows a message instead of crashing"
  artifacts:
    - path: "screener/display.py"
      provides: "render_results_table, render_stage_summary, render_filter_breakdown, formatting helpers"
      exports: ["render_results_table", "render_stage_summary", "render_filter_breakdown", "fmt_large_number", "fmt_price", "fmt_pct", "fmt_ratio"]
    - path: "tests/test_display.py"
      provides: "Unit tests for all display functions and formatters"
    - path: "pyproject.toml"
      provides: "rich>=14.0 dependency"
      contains: "rich"
  key_links:
    - from: "screener/display.py"
      to: "models/screened_stock.py"
      via: "imports ScreenedStock, FilterResult"
      pattern: "from models\\.screened_stock import"
    - from: "screener/display.py"
      to: "rich"
      via: "Rich Table, Panel, Console"
      pattern: "from rich\\."
---

<objective>
Create the screener display module with Rich-formatted results table and filter elimination summaries.

Purpose: Users need clear, informative terminal output from the screening pipeline -- a formatted table showing all passing stocks ranked by score, and a funnel summary showing how many stocks were eliminated at each filtering stage.

Output: `screener/display.py` with three rendering functions (results table, stage summary panel, per-filter breakdown table), number formatting helpers, and comprehensive tests.
</objective>

<execution_context>
@/Users/djbeatbug/.claude/get-shit-done/workflows/execute-plan.md
@/Users/djbeatbug/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-output-and-display/04-CONTEXT.md
@.planning/phases/04-output-and-display/04-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From models/screened_stock.py:
```python
@dataclass
class FilterResult:
    filter_name: str
    passed: bool
    actual_value: Optional[float] = None
    threshold: Optional[float] = None
    reason: str = ""

@dataclass
class ScreenedStock:
    symbol: str
    price: Optional[float] = None
    avg_volume: Optional[float] = None
    market_cap: Optional[float] = None
    debt_equity: Optional[float] = None
    net_margin: Optional[float] = None
    sales_growth: Optional[float] = None
    sector: Optional[str] = None
    industry: Optional[str] = None
    rsi_14: Optional[float] = None
    sma_200: Optional[float] = None
    above_sma200: Optional[bool] = None
    hv_30: Optional[float] = None
    is_optionable: Optional[bool] = None
    score: Optional[float] = None
    filter_results: list[FilterResult] = field(default_factory=list)

    @property
    def passed_all_filters(self) -> bool: ...
    @property
    def failed_filters(self) -> list[FilterResult]: ...
```

From screener/pipeline.py:
```python
def run_pipeline(trade_client, stock_client, finnhub_client, config, symbol_list_path="config/symbol_list.txt") -> list[ScreenedStock]:
    """Returns ALL ScreenedStock objects (passing + eliminated), sorted by score descending."""
```

Filter names used in FilterResult.filter_name:
- Stage 0: "bar_data"
- Stage 1: "price_range", "avg_volume", "rsi", "sma200"
- Stage 2: "market_cap", "debt_equity", "net_margin", "sales_growth", "sector", "optionable"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Number formatting helpers and results table renderer</name>
  <files>pyproject.toml, screener/display.py, tests/test_display.py</files>
  <behavior>
    - fmt_large_number(2100000000) -> "$2.1B"
    - fmt_large_number(3200000, prefix="") -> "3.2M"
    - fmt_large_number(45000, prefix="$") -> "$45.0K"
    - fmt_large_number(None) -> "N/A"
    - fmt_price(24.5) -> "$24.50"
    - fmt_price(None) -> "N/A"
    - fmt_pct(12.345) -> "12.3%"
    - fmt_pct(None) -> "N/A"
    - fmt_ratio(0.75) -> "0.75"
    - fmt_ratio(None) -> "N/A"
    - _score_style with 3+ stocks distributes green/yellow/red by thirds
    - _score_style with 1 stock returns "green"
    - _score_style with 0 scores returns "white"
    - render_results_table with passing stocks outputs Rich Table with 10 columns and numbered rows
    - render_results_table with 0 passing stocks prints "No stocks passed all filters" message
    - render_results_table only shows stocks where passed_all_filters=True and score is not None
    - Table rows sorted by score descending
    - Score values appear with color markup ([green], [yellow], [red])
  </behavior>
  <action>
    1. Add "rich>=14.0" to pyproject.toml dependencies list (after existing deps).

    2. Create screener/display.py with:
       - `import logging as stdlib_logging` (follow project pattern to avoid logging/ shadow)
       - Import from rich: Console, Table, box, Panel
       - Import from models.screened_stock: ScreenedStock, FilterResult
       - Module-level `_default_console = Console()`

    3. Implement pure formatting helpers (no Rich dependency):
       - `fmt_large_number(value: float | None, prefix: str = "$") -> str` -- handles B/M/K thresholds, None -> "N/A"
       - `fmt_price(value: float | None) -> str` -- "$X.XX" format, None -> "N/A"
       - `fmt_pct(value: float | None) -> str` -- "X.X%" format, None -> "N/A"
       - `fmt_ratio(value: float | None) -> str` -- "X.XX" format, None -> "N/A"

    4. Implement `_score_style(score: float, all_scores: list[float]) -> str`:
       - Sort all_scores, compute thirds boundaries
       - >= high cutoff -> "green", >= low cutoff -> "yellow", else -> "red"
       - Edge cases: empty list -> "white", fewer than 3 -> all "green"

    5. Implement `render_results_table(stocks: list[ScreenedStock], console: Console | None = None) -> None`:
       - Filter to passing stocks with score (passed_all_filters=True and score is not None)
       - Sort by score descending
       - If none: print "[yellow]No stocks passed all filters.[/yellow]" and return
       - Create Rich Table with title="Screening Results", box=box.ROUNDED, header_style="bold cyan", row_styles=["", "dim"]
       - 10 columns: # (right, dim, width=4), Symbol (bold, no_wrap), Price (right), AvgVol (right), MktCap (right), D/E (right), Margin (right), Growth (right), RSI (right), Score (right), Sector (max_width=20)
       - Note: that's 11 columns total counting the row number "#" column -- this matches the user spec of "10 data columns + numbered rows"
       - Each row: numbered index, symbol, fmt_price, fmt_large_number(vol, prefix=""), fmt_large_number(market_cap), fmt_ratio(d/e), fmt_pct(margin), fmt_pct(growth), fmt_pct(rsi), score with color markup, sector or "N/A"

    6. Create tests/test_display.py:
       - Use `Console(file=StringIO(), width=120)` to capture output for assertions
       - Test class TestFormatters: test each formatter with normal values, None, edge cases (0, negative, boundary values like exactly 1B)
       - Test class TestScoreStyle: test thirds distribution, single stock, empty list, two stocks
       - Test class TestRenderResultsTable: test with 3+ passing stocks (verify column headers present, row count, score colors in markup), test with 0 passing stocks (verify "No stocks passed" message), test that only passing+scored stocks appear
       - Build test ScreenedStock fixtures using ScreenedStock.from_symbol() then populating fields manually

    Important: Run tests from /tmp to avoid the logging/ shadow issue:
    `cd /tmp && python -m pytest /path/to/tests/test_display.py -x`
  </action>
  <verify>
    <automated>cd /tmp && python -m pytest /Users/djbeatbug/RoadToMillion/wheeely/tests/test_display.py::TestFormatters /Users/djbeatbug/RoadToMillion/wheeely/tests/test_display.py::TestScoreStyle /Users/djbeatbug/RoadToMillion/wheeely/tests/test_display.py::TestRenderResultsTable -x -v</automated>
  </verify>
  <done>
    - pyproject.toml has rich>=14.0 in dependencies
    - screener/display.py exists with 4 formatting helpers, _score_style, and render_results_table
    - All formatter tests pass (normal values, None, edge cases)
    - Results table renders with correct 10 data columns + numbered rows
    - Score color-coding distributes by thirds of actual score distribution
    - Empty results show message, no crash
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Filter elimination summaries (stage summary panel and per-filter breakdown)</name>
  <files>screener/display.py, tests/test_display.py</files>
  <behavior>
    - render_stage_summary shows panel with Universe, After bars, Stage 1, Stage 2, Scored counts
    - render_stage_summary shows reduction counts (e.g., "(-5)") between stages
    - render_stage_summary handles 0 stocks gracefully (all zeros)
    - render_filter_breakdown shows table with Filter, Removed, Remaining columns
    - render_filter_breakdown only shows filters that actually removed stocks (failed > 0)
    - render_filter_breakdown shows waterfall: each row's remaining = previous remaining - removed
    - render_filter_breakdown with all-passing stocks shows empty table or minimal output
  </behavior>
  <action>
    1. Add to screener/display.py:

    2. Implement `render_stage_summary(stocks: list[ScreenedStock], console: Console | None = None) -> None`:
       - Count total stocks (universe)
       - Count stocks without bar_data failure = had_bars
       - Count stage 1 passers: stocks that have stage 1 filter results AND all stage 1 filters passed. Stage 1 filter names: {"price_range", "avg_volume", "rsi", "sma200"}
       - Count stage 2 passers: stocks where passed_all_filters is True
       - Count scored: stocks where score is not None
       - Build multi-line text with aligned counts and (-N) reduction at each stage
       - Wrap in Rich Panel with title="Filter Summary", border_style="blue", expand=False
       - console.print(panel)

    3. Implement `render_filter_breakdown(stocks: list[ScreenedStock], console: Console | None = None) -> None`:
       - Define filter_order: ["bar_data", "price_range", "avg_volume", "rsi", "sma200", "market_cap", "debt_equity", "net_margin", "sales_growth", "sector", "optionable"]
       - Create Rich Table with title="Filter Breakdown", box=box.SIMPLE_HEAVY, header_style="bold"
       - 3 columns: Filter (cyan), Removed (right, red), Remaining (right, green)
       - Start with remaining = len(stocks)
       - For each filter in order: count stocks with that filter_name and passed=False, subtract from remaining, add row only if failed > 0
       - console.print(table)

    4. Add tests to tests/test_display.py:
       - Test class TestRenderStageSummary:
         - Build a list of ~10 ScreenedStock fixtures with various filter outcomes:
           - 2 with bar_data failure (no further filters)
           - 2 failing stage 1 filters (price_range or volume)
           - 3 failing stage 2 filters (market_cap or sector)
           - 3 passing all filters with scores
         - Verify captured output contains "Filter Summary" title
         - Verify counts appear correctly in output (e.g., "10" for universe, correct stage counts)
         - Test with empty stock list (0 stocks)

       - Test class TestRenderFilterBreakdown:
         - Use same fixture set as stage summary
         - Verify "Filter Breakdown" title in output
         - Verify filter names that removed stocks appear in output
         - Verify filters that removed 0 stocks do NOT appear in output
         - Verify remaining count is a proper waterfall (decreasing)

       - Build helper function `_make_stock(symbol, filter_results, score=None)` to reduce fixture boilerplate

    Important: Run tests from /tmp to avoid the logging/ shadow issue.
  </action>
  <verify>
    <automated>cd /tmp && python -m pytest /Users/djbeatbug/RoadToMillion/wheeely/tests/test_display.py::TestRenderStageSummary /Users/djbeatbug/RoadToMillion/wheeely/tests/test_display.py::TestRenderFilterBreakdown -x -v</automated>
  </verify>
  <done>
    - render_stage_summary displays Rich Panel with Universe/Stage 1/Stage 2/Scored counts and reduction numbers
    - render_filter_breakdown displays Rich Table with per-filter waterfall of removed/remaining
    - Only filters that actually removed stocks appear in breakdown
    - Empty stock list handled gracefully
    - All filter summary tests pass
  </done>
</task>

</tasks>

<verification>
Full test suite for display module:
```bash
cd /tmp && python -m pytest /Users/djbeatbug/RoadToMillion/wheeely/tests/test_display.py -x -v
```

Full project test suite still passes:
```bash
cd /tmp && python -m pytest /Users/djbeatbug/RoadToMillion/wheeely/tests/ -x
```
</verification>

<success_criteria>
- Rich library installed and in pyproject.toml dependencies
- screener/display.py has: render_results_table, render_stage_summary, render_filter_breakdown, 4 formatting helpers
- Results table shows 10 data columns with numbered rows, compact formatting, color-coded scores
- Stage summary panel shows funnel from universe to scored with reduction counts
- Per-filter breakdown shows waterfall table with only filters that removed stocks
- All display functions accept optional Console parameter for testability
- All tests pass from /tmp (no logging shadow issues)
</success_criteria>

<output>
After completion, create `.planning/phases/04-output-and-display/04-01-SUMMARY.md`
</output>
