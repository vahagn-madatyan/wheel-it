---
phase: 01-foundation
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - screener/__init__.py
  - screener/config_loader.py
  - config/presets/conservative.yaml
  - config/presets/moderate.yaml
  - config/presets/aggressive.yaml
  - models/screened_stock.py
  - tests/__init__.py
  - tests/conftest.py
  - tests/test_screener_config.py
autonomous: true
requirements: [CONF-01, CONF-02, CONF-03, CONF-04]

must_haves:
  truths:
    - "User can create a screener.yaml with filter thresholds and the system loads it without error"
    - "User can select a preset profile (conservative, moderate, aggressive) and see different thresholds applied"
    - "User can override individual preset values in screener.yaml and the overrides take effect without clobbering other preset values"
    - "User receives clear, actionable error messages when screener.yaml contains invalid values"
    - "Missing screener.yaml auto-generates with moderate preset and logs a warning"
  artifacts:
    - path: "screener/config_loader.py"
      provides: "YAML loading, preset merging, Pydantic validation, auto-generation"
      exports: ["load_config", "ScreenerConfig", "FundamentalsConfig", "TechnicalsConfig"]
    - path: "config/presets/moderate.yaml"
      provides: "Moderate preset with Finviz baseline values"
      contains: "market_cap_min"
    - path: "config/presets/conservative.yaml"
      provides: "Conservative preset with tight fundamentals"
      contains: "market_cap_min"
    - path: "config/presets/aggressive.yaml"
      provides: "Aggressive preset with loose fundamentals"
      contains: "market_cap_min"
    - path: "models/screened_stock.py"
      provides: "ScreenedStock and FilterResult dataclasses"
      exports: ["ScreenedStock", "FilterResult"]
    - path: "tests/test_screener_config.py"
      provides: "Unit tests for config loading and validation"
      min_lines: 80
  key_links:
    - from: "screener/config_loader.py"
      to: "config/presets/*.yaml"
      via: "load_preset() reads YAML files from config/presets/"
      pattern: "config/presets.*\\.yaml"
    - from: "screener/config_loader.py"
      to: "pydantic BaseModel"
      via: "ScreenerConfig.model_validate(merged)"
      pattern: "model_validate"
---

<objective>
Create the YAML config loading pipeline with preset profiles, Pydantic validation, and the ScreenedStock data model.

Purpose: Establish the configuration foundation so users can customize screening criteria through YAML files with preset profiles, and the system validates all input with clear error messages. Also define the ScreenedStock data model that later phases populate.
Output: Working config loader with 3 preset YAML files, Pydantic validation models, ScreenedStock dataclass, and comprehensive tests.
</objective>

<execution_context>
@/Users/djbeatbug/.claude/get-shit-done/workflows/execute-plan.md
@/Users/djbeatbug/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-VALIDATION.md

<interfaces>
<!-- Existing codebase patterns to follow -->

From config/credentials.py:
```python
from dotenv import load_dotenv
import os

load_dotenv(override=True)

ALPACA_API_KEY = os.getenv("ALPACA_API_KEY")
ALPACA_SECRET_KEY = os.getenv("ALPACA_SECRET_KEY")
IS_PAPER = os.getenv("IS_PAPER", "true").lower() == "true"
```

From models/contract.py (pattern for ScreenedStock):
```python
from typing import Optional
from dataclasses import dataclass, field

@dataclass
class Contract:
    underlying: str
    symbol: str
    contract_type: str
    dte: Optional[float] = None
    strike: Optional[float] = None
    delta: Optional[float] = None
    # ... Optional fields populated progressively
    client: Optional["BrokerClient"] = field(default=None, repr=False, compare=False)

    @classmethod
    def from_contract(cls, contract, client=None) -> "Contract": ...
    @classmethod
    def from_contract_snapshot(cls, contract, snapshot) -> "Contract": ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Test scaffolding, preset YAML files, and ScreenedStock data model</name>
  <files>
    tests/__init__.py,
    tests/conftest.py,
    tests/test_screener_config.py,
    config/presets/conservative.yaml,
    config/presets/moderate.yaml,
    config/presets/aggressive.yaml,
    models/screened_stock.py,
    screener/__init__.py
  </files>
  <behavior>
    - test_load_preset_moderate: loading "moderate" preset returns dict with fundamentals.market_cap_min=2000000000, fundamentals.debt_equity_max=1.0, fundamentals.net_margin_min=0, fundamentals.sales_growth_min=5, technicals.price_min=10, technicals.price_max=50, technicals.avg_volume_min=2000000, technicals.rsi_max=60, technicals.above_sma200=true
    - test_load_preset_conservative: loading "conservative" preset returns dict with fundamentals.market_cap_min=10000000000, fundamentals.debt_equity_max=0.5, fundamentals.net_margin_min=10, fundamentals.sales_growth_min=10
    - test_load_preset_aggressive: loading "aggressive" preset returns dict with fundamentals.market_cap_min=500000000, fundamentals.debt_equity_max=2.0, fundamentals.net_margin_min=-5, fundamentals.sales_growth_min=0
    - test_all_presets_share_technical_values: all three presets have identical technicals section (price $10-$50, avg_volume 2M, rsi 60, above_sma200 true)
    - test_screened_stock_from_symbol: ScreenedStock.from_symbol("AAPL") creates instance with symbol="AAPL" and all Optional fields as None
    - test_screened_stock_passed_all_filters: with mixed FilterResults, passed_all_filters returns False; with all passing, returns True
    - test_screened_stock_failed_filters: returns only the FilterResult entries where passed=False
  </behavior>
  <action>
    1. Install pytest: `uv pip install pytest`
    2. Create `tests/__init__.py` (empty).
    3. Create `tests/conftest.py` with fixtures:
       - `tmp_config_dir`: creates a temp directory with `presets/` subdirectory, yields the path, cleans up after
       - `sample_screener_yaml`: returns a valid screener.yaml string content with `preset: moderate` and one override (`fundamentals.market_cap_min: 5000000000`)
    4. Create `config/presets/` directory.
    5. Create `config/presets/moderate.yaml` with the Finviz baseline values per CONTEXT.md:
       ```yaml
       preset: moderate
       fundamentals:
         market_cap_min: 2000000000
         debt_equity_max: 1.0
         net_margin_min: 0
         sales_growth_min: 5
       technicals:
         price_min: 10
         price_max: 50
         avg_volume_min: 2000000
         rsi_max: 60
         above_sma200: true
       options:
         optionable: true
       sectors:
         include: []
         exclude: []
       ```
       NOTE: Use plain integers (no underscores) in YAML to avoid PyYAML parsing issues per RESEARCH.md pitfall 5.
    6. Create `config/presets/conservative.yaml` — same technicals/options/sectors, but fundamentals: market_cap_min=10000000000, debt_equity_max=0.5, net_margin_min=10, sales_growth_min=10.
    7. Create `config/presets/aggressive.yaml` — same technicals/options/sectors, but fundamentals: market_cap_min=500000000, debt_equity_max=2.0, net_margin_min=-5, sales_growth_min=0.
    8. Create `screener/__init__.py` (empty).
    9. Create `models/screened_stock.py` with:
       - `FilterResult` dataclass: filter_name (str), passed (bool), actual_value (Optional[float]), threshold (Optional[float]), reason (str, default "")
       - `ScreenedStock` dataclass following Contract pattern: symbol (str, required), then Optional fields for price, avg_volume, market_cap, debt_equity, net_margin, sales_growth, sector, industry, rsi_14, sma_200, above_sma200, is_optionable, score, filter_results (list[FilterResult], default_factory=list), raw_finnhub_profile (Optional[dict]), raw_finnhub_metrics (Optional[dict]), raw_alpaca_bars (Optional[dict])
       - `from_symbol(cls, symbol)` classmethod that returns `cls(symbol=symbol.upper())`
       - `passed_all_filters` property: returns False if filter_results empty, else all(r.passed for r)
       - `failed_filters` property: returns [r for r in filter_results if not r.passed]
    10. Write tests in `tests/test_screener_config.py` covering all behaviors listed above. Tests for preset loading should use `yaml.safe_load()` directly on the preset files (the `load_preset` function will be created in Task 2).
    11. Run tests — preset and ScreenedStock tests should pass (GREEN). Config loader tests will be written in Task 2.

    IMPORTANT: For stdlib logging in screener modules, use `import logging as stdlib_logging` to avoid the project's logging/ package shadow (per CLAUDE.md and RESEARCH.md pitfall 4).
  </action>
  <verify>
    <automated>cd /Users/djbeatbug/RoadToMillion/wheeely && python -m pytest tests/test_screener_config.py -x -q</automated>
  </verify>
  <done>
    - Three preset YAML files exist in config/presets/ with correct threshold values
    - Technical filter values are identical across all three presets
    - Fundamental values differ: conservative (tight), moderate (Finviz baseline), aggressive (loose)
    - ScreenedStock dataclass exists with progressive Optional fields and from_symbol constructor
    - FilterResult dataclass exists with pass/fail tracking
    - All tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Config loader with deep merge, Pydantic validation, and auto-generation</name>
  <files>
    screener/config_loader.py,
    tests/test_screener_config.py
  </files>
  <behavior>
    - test_load_valid_config: load_config with a valid screener.yaml returns ScreenerConfig with correct values
    - test_deep_merge_overrides: screener.yaml with only `fundamentals.market_cap_min: 5000000000` override keeps all other moderate preset values intact
    - test_deep_merge_preserves_nested: overriding one field in `technicals` does not clobber other technicals fields
    - test_auto_generate_missing_config: when screener.yaml does not exist, load_config auto-generates it with moderate preset and returns valid config
    - test_validation_error_wrong_type: screener.yaml with `fundamentals.market_cap_min: "not_a_number"` raises ValidationError with clear message
    - test_validation_error_out_of_range: screener.yaml with `fundamentals.debt_equity_max: -1` raises ValidationError
    - test_validation_error_invalid_preset: screener.yaml with `preset: "yolo"` raises ValidationError mentioning valid presets
    - test_preset_selection: screener.yaml with `preset: conservative` loads conservative values as base
    - test_format_validation_errors: ValidationError errors are formatted with field name and human-readable message
  </behavior>
  <action>
    1. Create `screener/config_loader.py` with:
       - Pydantic v2 models (use v2 API per RESEARCH.md pitfall 3):
         - `FundamentalsConfig(BaseModel)`: market_cap_min (int), debt_equity_max (float), net_margin_min (float), sales_growth_min (float). Validators: debt_equity_max >= 0 and <= 10, market_cap_min > 0, price fields positive.
         - `TechnicalsConfig(BaseModel)`: price_min (float), price_max (float), avg_volume_min (int), rsi_max (float), above_sma200 (bool). Validators: price_min > 0, price_max > price_min, rsi_max between 0-100.
         - `OptionsConfig(BaseModel)`: optionable (bool)
         - `SectorsConfig(BaseModel)`: include (list[str]), exclude (list[str])
         - `ScreenerConfig(BaseModel)`: preset (str), fundamentals (FundamentalsConfig), technicals (TechnicalsConfig), options (OptionsConfig), sectors (SectorsConfig). Validator: preset in {"conservative", "moderate", "aggressive"}.
       - `load_preset(preset_name: str) -> dict`: reads `config/presets/{preset_name}.yaml` via yaml.safe_load(). Raises FileNotFoundError with message listing valid presets if not found.
       - `deep_merge(base: dict, overrides: dict) -> dict`: recursive merge using copy.deepcopy. When both base[key] and overrides[key] are dicts, recurse. Otherwise overrides[key] wins.
       - `load_config(config_path: str = "config/screener.yaml") -> ScreenerConfig`:
         a. If path doesn't exist, call `_generate_default_config(path)` and log warning via stdlib logging: "No screener.yaml found, generated default with moderate preset"
         b. Parse YAML with `yaml.safe_load(f) or {}` (pitfall 1: None for empty files)
         c. Get preset name from user_config.get("preset", "moderate")
         d. Load preset via load_preset()
         e. Deep merge: preset is base, user config is overrides
         f. Validate via `ScreenerConfig.model_validate(merged)`
         g. Return validated config
       - `_generate_default_config(path: Path)`: uses `Path.mkdir(parents=True, exist_ok=True)` for parent dirs. Writes a commented YAML file with `preset: moderate` and example overrides commented out. Uses yaml.dump() for the data, but prepend a header comment explaining usage.
       - `format_validation_errors(e: ValidationError) -> str`: iterate `e.errors()`, format each as `"  {field_path}: {message}"` (e.g., "  fundamentals.debt_equity_max: must be >= 0"). Return joined string.

    2. Add tests to `tests/test_screener_config.py` covering all behaviors above. Use `tmp_path` fixture for temporary config files. Tests for auto-generation should verify the file is created AND the returned config is valid. Tests for validation errors should use `pytest.raises(ValidationError)` or check the formatted error output.

    3. For the logging shadow issue: in config_loader.py, import as `import logging as stdlib_logging` at the top of the file, then use `stdlib_logging.getLogger(__name__)` for the logger.

    4. Run full test suite — all tests must pass (GREEN).
  </action>
  <verify>
    <automated>cd /Users/djbeatbug/RoadToMillion/wheeely && python -m pytest tests/test_screener_config.py -x -q</automated>
  </verify>
  <done>
    - load_config() loads screener.yaml, merges with preset, validates via Pydantic, returns typed ScreenerConfig
    - Deep merge correctly handles partial overrides without clobbering sibling values
    - Missing screener.yaml is auto-generated with moderate preset
    - Invalid values produce clear, actionable Pydantic validation errors
    - Invalid preset name produces error listing valid options
    - All tests pass
  </done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_screener_config.py -x -q` -- all config and model tests pass
- `python -c "from screener.config_loader import load_config; c = load_config('config/presets/moderate.yaml'); print(c.fundamentals.market_cap_min)"` -- prints 2000000000
- `python -c "from models.screened_stock import ScreenedStock; s = ScreenedStock.from_symbol('AAPL'); print(s.symbol, s.price)"` -- prints "AAPL None"
</verification>

<success_criteria>
- Three preset YAML files exist with correct differentiated fundamental values and identical technical values
- ScreenerConfig Pydantic model validates config with clear errors on invalid input
- load_config() handles: valid config, partial overrides, missing file (auto-generate), invalid values
- Deep merge preserves unoverridden nested values
- ScreenedStock dataclass follows Contract pattern with Optional progressive fields
- All tests in test_screener_config.py pass
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
