---
phase: 05-cli-and-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - core/cli_common.py
  - screener/export.py
  - tests/test_export.py
autonomous: true
requirements:
  - OUTP-03
  - SAFE-03
  - CLI-03

must_haves:
  truths:
    - "Screened symbols can be written to config/symbol_list.txt via export function"
    - "Symbols with active positions (short puts, assigned shares, short calls) are never removed from symbol_list.txt"
    - "A colored diff is displayed showing added, removed, and protected symbols before writing"
    - "Zero screener results skips file write and prints warning"
    - "Missing Alpaca credentials produce a hard error before any export attempt"
  artifacts:
    - path: "screener/export.py"
      provides: "Position-safe symbol list export with diff display"
      exports: ["get_protected_symbols", "export_symbols"]
    - path: "core/cli_common.py"
      provides: "Shared CLI credential helpers for both entry points"
      exports: ["require_alpaca_credentials", "create_broker_client"]
    - path: "tests/test_export.py"
      provides: "Tests for symbol export with position protection"
      min_lines: 80
  key_links:
    - from: "screener/export.py"
      to: "core/state_manager.py"
      via: "update_state() call for position detection"
      pattern: "update_state\\(positions\\)"
    - from: "screener/export.py"
      to: "config/symbol_list.txt"
      via: "Path.write_text for symbol list output"
      pattern: "write_text"
    - from: "core/cli_common.py"
      to: "config/credentials.py"
      via: "imports ALPACA_API_KEY, ALPACA_SECRET_KEY, IS_PAPER"
      pattern: "from config\\.credentials import"
---

<objective>
Create the symbol export module with position-safe merge logic and the shared CLI helpers that both `run-screener` and `run-strategy` will use.

Purpose: The export module is the core safety mechanism -- it ensures active wheel positions are never removed from the symbol list when updating with screener results. The CLI common module extracts shared credential validation used by both entry points. These must exist before the CLI entry points are wired in Plan 02.

Output: `screener/export.py`, `core/cli_common.py`, `tests/test_export.py`, updated `pyproject.toml` with typer dependency.
</objective>

<execution_context>
@/Users/djbeatbug/.claude/get-shit-done/workflows/execute-plan.md
@/Users/djbeatbug/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-cli-and-integration/05-CONTEXT.md
@.planning/phases/05-cli-and-integration/05-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From core/state_manager.py:
```python
def update_state(all_positions):
    """Given current positions, return state dict: {symbol: {"type": "short_put"|"long_shares"|"short_call", ...}}"""
```

From config/credentials.py:
```python
ALPACA_API_KEY = os.getenv("ALPACA_API_KEY")
ALPACA_SECRET_KEY = os.getenv("ALPACA_SECRET_KEY")
IS_PAPER = os.getenv("IS_PAPER", "true").lower() == "true"
FINNHUB_API_KEY = os.getenv("FINNHUB_API_KEY")
def require_finnhub_key() -> str: ...
```

From core/broker_client.py:
```python
class BrokerClient:
    def __init__(self, api_key, secret_key, paper): ...
    def get_positions(self): ...  # Returns list of Alpaca Position objects
```

From models/screened_stock.py:
```python
@dataclass
class ScreenedStock:
    symbol: str
    score: Optional[float] = None
    filter_results: list[FilterResult] = field(default_factory=list)
    @property
    def passed_all_filters(self) -> bool: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install Typer and create shared CLI helpers</name>
  <files>pyproject.toml, core/cli_common.py</files>
  <behavior>
    - require_alpaca_credentials() returns (key, secret, is_paper) tuple when all env vars are set
    - require_alpaca_credentials() raises SystemExit with actionable message when ALPACA_API_KEY is missing
    - require_alpaca_credentials() raises SystemExit with actionable message when ALPACA_SECRET_KEY is missing
    - create_broker_client() returns a BrokerClient instance when credentials are valid
  </behavior>
  <action>
    1. Add `"typer>=0.9.0"` to the `dependencies` list in `pyproject.toml`. Run `uv pip install -e .` to install.
    2. Create `core/cli_common.py` with:
       - `import logging as stdlib_logging` (CRITICAL: avoid logging/ shadow)
       - `require_alpaca_credentials() -> tuple[str, str, bool]`: checks ALPACA_API_KEY and ALPACA_SECRET_KEY from config.credentials, raises SystemExit with message "Error: --update-symbols requires Alpaca credentials for position protection.\nSet ALPACA_API_KEY and ALPACA_SECRET_KEY in .env" if either is missing. Returns (key, secret, is_paper) tuple.
       - `create_broker_client() -> BrokerClient`: calls require_alpaca_credentials(), constructs and returns BrokerClient.
    3. Write tests in tests/test_export.py (test_cli_common section) using monkeypatch to mock credentials module-level vars, verifying both success and failure paths of require_alpaca_credentials.
  </action>
  <verify>
    <automated>cd /tmp && python -m pytest /Users/djbeatbug/RoadToMillion/wheeely/tests/test_export.py -x -q -k "cli_common"</automated>
  </verify>
  <done>Typer installed, cli_common.py exports require_alpaca_credentials and create_broker_client, tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create position-safe symbol export module</name>
  <files>screener/export.py, tests/test_export.py</files>
  <behavior>
    - get_protected_symbols returns dict mapping symbol to wheel state type from positions list
    - export_symbols with screened symbols writes sorted union of screened + protected to file
    - export_symbols prints green "+SYM" for newly added symbols
    - export_symbols prints red "-SYM (screened out)" for removed symbols
    - export_symbols prints yellow "~SYM: kept (active short put)" for protected symbols
    - export_symbols with empty screened list AND no protected symbols returns False and prints warning
    - export_symbols with empty screened list BUT protected symbols still writes protected symbols
    - export_symbols preserves protected symbols even when they are not in screened list
    - File format is one symbol per line, sorted alphabetically, trailing newline
  </behavior>
  <action>
    1. Create `screener/export.py` with:
       - `import logging as stdlib_logging` (CRITICAL: avoid logging/ shadow)
       - `from pathlib import Path` and `from rich.console import Console`
       - `get_protected_symbols(positions, update_state_fn) -> dict[str, str]`: takes Alpaca positions list and the `update_state` function, calls `update_state_fn(positions)`, returns dict mapping symbol -> state type string (e.g., "short_put", "long_shares", "short_call"). Accept `update_state_fn` as parameter (not import directly) for testability.
       - `export_symbols(screened: list[str], protected: dict[str, str], path: Path, console: Console | None = None) -> bool`:
         - If `screened` is empty AND `protected` is empty: print yellow warning "Warning: screener found 0 passing stocks. Using existing symbol_list.txt.", return False.
         - Read current symbols from `path` if file exists (skip empty lines and lines starting with #).
         - Build final set: `set(screened) | set(protected.keys())`.
         - Compute diff: added = final - current, removed = current - final, kept_protected = protected.keys() & current.
         - Print diff with Rich markup: `[green]+{sym}[/green]` for added, `[red]-{sym} (screened out)[/red]` for removed, `[yellow]~{sym}: kept (active {state_type})[/yellow]` for protected (replace underscores with spaces in state_type).
         - Write `path` with sorted final symbols, one per line, trailing newline.
         - Return True.
    2. Add comprehensive tests to `tests/test_export.py`:
       - `test_protected_symbols_kept`: screened list omits a symbol that has active position -> final file contains it with warning printed.
       - `test_export_writes_file`: basic happy path, file written with correct content.
       - `test_zero_results_skips_write`: empty screened + empty protected -> returns False, file not modified.
       - `test_diff_display`: verify green/red/yellow markup appears in console output using StringIO Console.
       - `test_get_protected_symbols`: mock update_state, verify correct dict mapping returned.
       - `test_existing_file_merged`: existing file has symbols, screened adds some, removes others, protected keeps some.
    3. All tests use `tmp_path` fixture for file operations, `Console(file=StringIO())` for output capture.
  </action>
  <verify>
    <automated>cd /tmp && python -m pytest /Users/djbeatbug/RoadToMillion/wheeely/tests/test_export.py -x -q</automated>
  </verify>
  <done>export_symbols writes position-safe symbol list with colored diff display, get_protected_symbols maps positions to state types, all tests pass including zero-result guard and protected symbol preservation</done>
</task>

</tasks>

<verification>
1. `cd /tmp && python -m pytest /Users/djbeatbug/RoadToMillion/wheeely/tests/test_export.py -v` -- all export and cli_common tests pass
2. `python -c "import typer; print(typer.__version__)"` -- typer is installed
3. `python -c "from core.cli_common import require_alpaca_credentials, create_broker_client"` -- imports work
4. `python -c "from screener.export import get_protected_symbols, export_symbols"` -- imports work
</verification>

<success_criteria>
- Typer dependency added to pyproject.toml and installed
- `core/cli_common.py` provides credential validation with hard error on missing keys
- `screener/export.py` writes symbol_list.txt with position protection, colored diff, and zero-result guard
- All tests in `tests/test_export.py` pass (6+ test functions covering CLI-03, OUTP-03, SAFE-03)
</success_criteria>

<output>
After completion, create `.planning/phases/05-cli-and-integration/05-01-SUMMARY.md`
</output>
